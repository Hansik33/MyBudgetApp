<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="MyBudgetApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyBudgetApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Budżet Domowy">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <ContentControl x:Name="MainContent" x:FieldModifier="public"
                        HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />

        <Button Click="InfoButton_Click"  IsTabStop="False"
                Width="48" Height="48" Margin="0,0,32,32" CornerRadius="100"
                HorizontalAlignment="Right" VerticalAlignment="Bottom"
                ToolTipService.ToolTip="Info">
            <FontIcon Glyph="&#xE946;" FontFamily="Segoe MDL2 Assets"/>
        </Button>
    </Grid>

</Window>