<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyBudgetApp.Controls.Dashboard.SavingCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyBudgetApp.Controls.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MyBudgetApp.ViewModels.Dashboard" 
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:SavingViewModel}">

    <Border BorderThickness="1" BorderBrush="DarkGray" Padding="6" Margin="0,4,0,4" CornerRadius="4">
        <StackPanel Orientation="Vertical" Spacing="2">
            <local:DeleteIconButton />
            <TextBlock>
            <Run Text="Cel:" FontWeight="Bold"/>
            <Run Text="{Binding Goal}" />
            </TextBlock>
            <TextBlock>
            <Run Text="Kwota:" FontWeight="Bold"/>
            <Run Text="{Binding Amount}" />
            <Run Text="zł" />
            </TextBlock>
            <TextBlock>
            <Run Text="Data:" FontWeight="Bold"/>
            <Run Text="{Binding Date}" />
            </TextBlock>
            <TextBlock>
            <Run Text="Godzina:" FontWeight="Bold"/>
            <Run Text="{Binding Time}" />
            </TextBlock>
        </StackPanel>
    </Border>

</UserControl>