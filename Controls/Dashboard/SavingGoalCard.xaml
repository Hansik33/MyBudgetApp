<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="MyBudgetApp.Controls.Dashboard.SavingGoalCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MyBudgetApp.Controls.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:MyBudgetApp.ViewModels.Dashboard" 
    mc:Ignorable="d"
    d:DataContext="{d:DesignInstance Type=viewmodels:SavingGoalViewModel}">

    <Border Background="{ThemeResource DashboardCardBackgroundAcrylic}"
            BorderBrush="{ThemeResource DashboardCardBorderBrush}"
            BorderThickness="1"
            Padding="6" Margin="0,4,0,4" 
            CornerRadius="4">
        <StackPanel Orientation="Vertical" 
                    Spacing="4">
            <local:DeleteIconButton
                Command="{x:Bind DeleteCommand, Mode=OneWay}"
                CommandParameter="{x:Bind SavingGoal, Mode=OneWay}" />
            <TextBlock>
            <Run Text="Cel:" FontWeight="Bold" />
            <Run Text="{Binding Name}" />
            </TextBlock>
            <TextBlock>
            <Run Text="Termin:" FontWeight="Bold" />
            <Run Text="{Binding Deadline}" />
            </TextBlock>

            <TextBlock Text="Postęp:" FontWeight="Bold" />
            <TextBlock Text="{Binding ProgressPercent}" 
                       HorizontalAlignment="Center" />
            <ProgressBar Minimum="0"
                         Maximum="{Binding TargetAmountAsDouble}" 
                         Value="{Binding SavedAmountAsDouble}"
                         Foreground="{Binding ProgressBrush}"
                         Height="8" />
            <TextBlock Text="{Binding ProgressAmount}" 
                       HorizontalAlignment="Center" />
        </StackPanel>
    </Border>

</UserControl>